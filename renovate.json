{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>liverty-music/.github:renovate-config"
  ],
  "constraintsFiltering": "strict",
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "packageRules": [
    {
      "description": "Go `tool` directive entries, grouped to keep build tooling to one pull request rather than four. These are independent CLIs and are NOT version-coupled — nothing breaks if they are split, so this grouping may be undone at any time. Stated explicitly because two neighbouring groups were removed for being presented as coupling when they were not.",
      "matchManagers": [
        "gomod"
      ],
      "matchPackageNames": [
        "github.com/bufbuild/buf/**",
        "github.com/go-delve/delve/**",
        "github.com/vektra/mockery/**",
        "mvdan.cc/gofumpt"
      ],
      "groupName": "go-tools"
    },
    {
      "description": "Disable ALL BSR-generated Go modules — but for two different reasons, and the difference decides what happens if Renovate changes. `liverty-music/schema/*` is excluded BY POLICY: it is advanced by the OpenSpec change that alters the schema, together with the code migration that change requires, and backend and frontend must sit on the same build. That stays true regardless of tooling. `pocketsign/apis/*` is excluded BY INABILITY. Design D7 deliberately kept it in scope — third-party schema, someone else's cadence, falling behind is the risk — but the first Renovate run failed both modules with `Could not determine new digest for update` and the failure took the version update with it; newer builds existed and none was proposed. The BSR version format `v1.20.0-20260826021924-0ff29b2b0335.1` resembles a Go pseudo-version, so Renovate splits the embedded commit out as a digest and cannot resolve a new one, the module being generated output with no repository behind it. `digest: {enabled: false}` was tried and did not help — the parse happens before the update type is chosen. IF RENOVATE GAINS BSR SUPPORT: re-enable pocketsign, keep liverty-music excluded. Left enabled-but-failing this would be the worst case — an axis that looks automated while proposing nothing, indistinguishable from one that is up to date. It is a known gap with a manual bump in the runbook, alongside buf.lock, which shares its root cause. `enabled: false` rather than `ignoreDeps` so they stay on the dashboard as disabled-with-an-update-available.",
      "matchPackageNames": [
        "buf.build/gen/go/**"
      ],
      "enabled": false
    }
  ]
}
